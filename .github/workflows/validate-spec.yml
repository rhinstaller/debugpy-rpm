name: Validate RPM Spec

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    container:
      image: fedora:rawhide

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          dnf install -y rpm-build rpmdevtools rpmlint spectool
          dnf builddep -y python3-debugpy.spec

      - name: Validate spec file
        run: |
          rpmlint python3-debugpy.spec || true

      - name: Download sources
        run: |
          spectool -g python3-debugpy.spec

      - name: Build SRPM
        run: |
          rpmbuild -bs python3-debugpy.spec \
            --define "_sourcedir $PWD" \
            --define "_srcrpmdir $PWD"
